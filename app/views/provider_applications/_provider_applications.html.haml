- provider_applications.each do |app|
  %li.provider_application.toggle_container{ :class => "#{app.wm_state.downcase}",
                                             :class => "{{(providerApps|findById:'#{app.id}').wm_state|lowercase}}" }
    = render_provider_partial("provider_application", { :app => app })
