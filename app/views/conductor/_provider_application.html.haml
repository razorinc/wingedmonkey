%ul.controls
  %li.status= app.state
  %li.stop= button_to 'Stop', provider_application_path(app.id), :method => :delete, :confirm => 'Are you sure you want to stop this application?', :class => 'button danger'
%h5
  -#= link_to app.name, provider_application_path(app.id)
  = app.name
  = "[ID: #{app.id}]"
%dl.properties
  %dt Launchable
  %dd= "Unknown"
