#heading My Applications

- if current_provider
  - if @provider_applications.empty?
    = render :partial => "getting_started"
  - else
    = render :partial => "app_list"
- else
  = render :partial => "choose_provider"
