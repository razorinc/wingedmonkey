- @launchables.each do |launchable|
  .box{:style => "margin-bottom: 10px"}
    .blueprint
      .left
        .app-name= launchable.name
        .bp-description= launchable.description
      .right
        .stats= "#{bp.cores} CPU / #{bp.memory.to_i / 1024 / 1024}MB Memory"
    .control{:style => "display:block"}
      = form_tag("/launch/launch", :method => "post") do
        = hidden_field_tag(:template_id, launchable.id)
        = submit_tag("Launch")
